/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { RFCDate } from "../../types/rfcdate.js";

export type GetPlanMonthRequest = {
  /**
   * The id of the plan. "last-used" can be used to specify the last used plan and "default" can be used if default plan selection is enabled (see: https://api.ynab.com/#oauth-default-plan).
   */
  planId: string;
  /**
   * The plan month in ISO format (e.g. 2016-12-01) ("current" can also be used to specify the current calendar month (UTC))
   */
  month: RFCDate;
};

/** @internal */
export type GetPlanMonthRequest$Outbound = {
  plan_id: string;
  month: string;
};

/** @internal */
export const GetPlanMonthRequest$outboundSchema: z.ZodType<
  GetPlanMonthRequest$Outbound,
  z.ZodTypeDef,
  GetPlanMonthRequest
> = z.object({
  planId: z.string(),
  month: z.instanceof(RFCDate).transform(v => v.toString()),
}).transform((v) => {
  return remap$(v, {
    planId: "plan_id",
  });
});

export function getPlanMonthRequestToJSON(
  getPlanMonthRequest: GetPlanMonthRequest,
): string {
  return JSON.stringify(
    GetPlanMonthRequest$outboundSchema.parse(getPlanMonthRequest),
  );
}
