/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { RFCDate } from "../types/rfcdate.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type MoneyMovementGroup = {
  id: string;
  /**
   * When the money movement group was created
   */
  groupCreatedAt: Date;
  /**
   * The month of the money movement group in ISO format (e.g. 2024-01-01)
   */
  month: RFCDate;
  note?: string | null | undefined;
  /**
   * The id of the user who performed the money movement group
   */
  performedByUserId?: string | null | undefined;
};

/** @internal */
export const MoneyMovementGroup$inboundSchema: z.ZodType<
  MoneyMovementGroup,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  group_created_at: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ),
  month: z.string().transform(v => new RFCDate(v)),
  note: z.nullable(z.string()).optional(),
  performed_by_user_id: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "group_created_at": "groupCreatedAt",
    "performed_by_user_id": "performedByUserId",
  });
});

export function moneyMovementGroupFromJSON(
  jsonString: string,
): SafeParseResult<MoneyMovementGroup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MoneyMovementGroup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MoneyMovementGroup' from JSON`,
  );
}
