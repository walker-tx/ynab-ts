/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  CurrencyFormat,
  CurrencyFormat$inboundSchema,
} from "./currencyformat.js";
import { DateFormat, DateFormat$inboundSchema } from "./dateformat.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type PlanSettings = {
  /**
   * The date format setting for the plan.  In some cases the format will not be available and will be specified as null.
   */
  dateFormat: DateFormat | null;
  /**
   * The currency format setting for the plan.  In some cases the format will not be available and will be specified as null.
   */
  currencyFormat: CurrencyFormat | null;
};

/** @internal */
export const PlanSettings$inboundSchema: z.ZodType<
  PlanSettings,
  z.ZodTypeDef,
  unknown
> = z.object({
  date_format: z.nullable(DateFormat$inboundSchema),
  currency_format: z.nullable(CurrencyFormat$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "date_format": "dateFormat",
    "currency_format": "currencyFormat",
  });
});

export function planSettingsFromJSON(
  jsonString: string,
): SafeParseResult<PlanSettings, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlanSettings$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlanSettings' from JSON`,
  );
}
