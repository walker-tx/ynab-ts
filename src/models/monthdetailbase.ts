/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { RFCDate } from "../types/rfcdate.js";
import { CategoryBase, CategoryBase$inboundSchema } from "./categorybase.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type MonthDetailBase = {
  month: RFCDate;
  note?: string | null | undefined;
  /**
   * The total amount of transactions categorized to 'Inflow: Ready to Assign' in the month
   */
  income: number;
  /**
   * The total amount assigned (budgeted) in the month
   */
  budgeted: number;
  /**
   * The total amount of transactions in the month, excluding those categorized to 'Inflow: Ready to Assign'
   */
  activity: number;
  /**
   * The available amount for 'Ready to Assign'
   */
  toBeBudgeted: number;
  /**
   * The Age of Money as of the month
   */
  ageOfMoney?: number | null | undefined;
  /**
   * Whether or not the month has been deleted.  Deleted months will only be included in delta requests.
   */
  deleted: boolean;
  /**
   * The plan month categories.  Amounts (budgeted, activity, balance, etc.) are specific to the {month} parameter specified.
   */
  categories: Array<CategoryBase>;
};

/** @internal */
export const MonthDetailBase$inboundSchema: z.ZodType<
  MonthDetailBase,
  z.ZodTypeDef,
  unknown
> = z.object({
  month: z.string().transform(v => new RFCDate(v)),
  note: z.nullable(z.string()).optional(),
  income: z.number().int(),
  budgeted: z.number().int(),
  activity: z.number().int(),
  to_be_budgeted: z.number().int(),
  age_of_money: z.nullable(z.number().int()).optional(),
  deleted: z.boolean(),
  categories: z.array(CategoryBase$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "to_be_budgeted": "toBeBudgeted",
    "age_of_money": "ageOfMoney",
  });
});

export function monthDetailBaseFromJSON(
  jsonString: string,
): SafeParseResult<MonthDetailBase, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MonthDetailBase$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MonthDetailBase' from JSON`,
  );
}
