/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  AccountType,
  AccountType$inboundSchema,
  AccountType$outboundSchema,
} from "./accounttype.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type SaveAccount = {
  /**
   * The name of the account
   */
  name: string;
  /**
   * The type of account
   */
  type: AccountType;
  /**
   * The current balance of the account in milliunits format
   */
  balance: number;
};

/** @internal */
export const SaveAccount$inboundSchema: z.ZodType<
  SaveAccount,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string(),
  type: AccountType$inboundSchema,
  balance: z.number().int(),
});

/** @internal */
export type SaveAccount$Outbound = {
  name: string;
  type: string;
  balance: number;
};

/** @internal */
export const SaveAccount$outboundSchema: z.ZodType<
  SaveAccount$Outbound,
  z.ZodTypeDef,
  SaveAccount
> = z.object({
  name: z.string(),
  type: AccountType$outboundSchema,
  balance: z.number().int(),
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace SaveAccount$ {
  /** @deprecated use `SaveAccount$inboundSchema` instead. */
  export const inboundSchema = SaveAccount$inboundSchema;
  /** @deprecated use `SaveAccount$outboundSchema` instead. */
  export const outboundSchema = SaveAccount$outboundSchema;
  /** @deprecated use `SaveAccount$Outbound` instead. */
  export type Outbound = SaveAccount$Outbound;
}

export function saveAccountToJSON(saveAccount: SaveAccount): string {
  return JSON.stringify(SaveAccount$outboundSchema.parse(saveAccount));
}

export function saveAccountFromJSON(
  jsonString: string,
): SafeParseResult<SaveAccount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SaveAccount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SaveAccount' from JSON`,
  );
}
